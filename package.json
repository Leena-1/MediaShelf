{
  "name": "movie-book-library",
  "version": "1.0.0",
  "description": "Movie & Book Library MERN application",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "server": "node server/server.js",
    "client": "npm run dev --prefix client",
    "dev": "node dev.js",
    "dev:concurrently": "concurrently --shell auto \"npm run server\" \"npm run client\"",
    "install-all": "npm install && npm install --prefix server && npm install --prefix client"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
